from gui.app_ui import App

# This condition checks if the script is executed directly
if __name__ == "__main__":
    app = App()  # Create an instance of our main application window
    app.mainloop() # Start the GUI event loop